/*let text = "Hello";

function checkText (){
	console.log("In the function",text); 
	text += " World";
	console.log("Still in the function",text);
}
console.log("before the function", text); //"Hello"


checkText()   //"In the function Hello"  // Still in the function Hello World

console.log("after the function", text); // after the function Hello World




let friend = "Harry";
function checkfriend (){
	let friend = "Alice";
	console.log("In the function",friend); 
	friend += " Smith";
	console.log("Still in the function",friend);
}

console.log("before the function", friend); // before the function Harry

checkfriend() // In the function Alice // In the function Alice Smith

console.log("after the function", friend); // after the function Harry*/

/*---------------------------------------------------------------------*/


/*function greeting(userName) {
   alert(userName)
}
greeting("John")

const greeting1 = function(userName) {
   alert(userName)
}
greeting1("John")


const greeting2 = (userName) => alert(userName)
greeting2("John")*/

/*function checkedAge (age) {
	if (age>=18) {
		return "You can drive"
	} else {
		return "You can't drive"
	}
}

const checkedAge1 = function (age) { 
	let result = age>=18 ? "You can drive" : "You";
	return result;
}



const checkedAge1 = (age) => age >= 18 ? "You can drive" : "You can't drive";*/

















